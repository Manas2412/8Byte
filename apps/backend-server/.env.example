# Database (for db package – sign-up, sign-in, profile)
DATABASE_URL="postgresql://postgres:postgres@127.0.0.1:5432/mydb"

# Redis (for cached-db – portfolio-enriched cache read)
# Use 127.0.0.1 if redis://localhost gives getaddrinfo ENOTFOUND
REDIS_URL="redis://127.0.0.1:6379"

# WS-server HTTP API (portfolio cache miss → backend forwards here)
# Must match ws-server (default 8081; use 8082 if WS_HTTP_PORT=8082 there)
WS_SERVER_URL="http://localhost:8082"
