# Env for running backend and ws-server inside Docker Compose.
# Use hostnames postgres and redis (Compose service names on the app network).
DATABASE_URL=postgresql://postgres:postgres@postgres:5432/mydb
REDIS_URL=redis://redis:6379
WS_SERVER_URL=http://ws-server:8081
